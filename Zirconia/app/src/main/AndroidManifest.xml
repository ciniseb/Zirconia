<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sebastien.zirconia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.PleinEcran">
        <activity
            android:name=".Partie"
            android:label="Partie"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MenuPrincipal"
            android:theme="@style/titre">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BanqueZirconienne"
            android:screenOrientation="portrait" />
        <activity android:name=".ListeJoueurs" />
        <activity android:name=".CompteBancaire" />
        <activity android:name=".Generique" />
        <activity android:name=".ListePlacements" />
        <activity android:name=".ListePlacementsSMEER"></activity>
    </application>

</manifest>